# ==========================================================
# EduGenie Learning Assistant — Python Dependencies
# Tested with Python 3.10+
# Install via: pip install -r requirements.txt
# ==========================================================

# --- Backend Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1        # ASGI server (includes websockets/httptools extras)

# --- Templating & Static Files ---
jinja2==3.1.4
python-multipart==0.0.9          # Required by FastAPI for form/file parsing

# --- AI Models ---
google-generativeai==0.7.2       # Gemini 1.5 Pro API client
transformers==4.42.4             # Hugging Face pipeline for LaMini-Flan-T5-783M
torch==2.3.1+cpu                 # CPU-only build (Mac M1 / resource-constrained friendly)
sentencepiece==0.2.0             # Tokenizer backend required by Flan-T5 models
accelerate==0.32.1               # Efficient local model loading/inference

# --- Environment & Config ---
python-dotenv==1.0.1              # For loading GEMINI_API_KEY from .env

# --- Database ---
sqlalchemy==2.0.31                # ORM layer (works with SQLite/PostgreSQL)

# --- Validation & Utilities ---
pydantic==2.8.2                   # Request/response schema validation (ships with FastAPI)

# ==========================================================
# NOTE (Mac M1 / Apple Silicon users):
# The "+cpu" torch build above is for x86 CPU-only installs.
# On Apple Silicon, instead run:
#   pip install torch torchvision torchaudio
# (the default PyPI build already supports M1/M2 via MPS backend)
# ==========================================================
